#name: Deploy to GitHub Pages
#
#on:
#  push:
#    branches:
#      - main
#      - feature/actions-pages-deployment  # Testing only, remove when merged
#  workflow_dispatch:
#
#permissions:
#  contents: read
#  pages: write
#  id-token: write
#
#concurrency:
#  group: "pages"
#  cancel-in-progress: false
#
#jobs:
#  build:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Setup Node.js
#        uses: actions/setup-node@v4
#        with:
#          node-version: '18'
#          cache: 'npm'
#
#      - name: Install Dependencies
#        run: npm ci
#
#      - name: Build News Page
#        run: npm run build:news
#
#      - name: Upload Pages Artifact
#        uses: actions/upload-pages-artifact@v3
#        with:
#          path: ./docs
#
#  deploy:
#    runs-on: ubuntu-latest
#    needs: build
#    environment:
#      name: github-pages
#      url: ${{ steps.deployment.outputs.page_url }}
#    steps:
#      - name: Deploy to GitHub Pages
#        id: deployment
#        uses: actions/deploy-pages@v4
